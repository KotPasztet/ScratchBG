// StageBG bits-like package.
// Import this when you want algorithmic helpers similar in spirit to C++ bits/stdc++.h.
// It does NOT contain solved olympiad tasks. It contains reusable containers and algorithms.
import "../std/runtime.sbg";
import "../std/core.sbg";
import "../std/math.sbg";
import "../std/strings.sbg";
import "../std/text.sbg";
import "../std/collections.sbg";
// C++'s <bits/stdc++.h> includes <iostream>, so cin >> / getline() must work
// from this package too (cin_get / getinput live in std/io.sbg).
import "../std/io.sbg";
import "./priority_queue.sbg";
import "./deque.sbg";
import "./dsu.sbg";
import "./fenwick.sbg";
import "./algorithms.sbg";
import "./cpp_compat.sbg";
