#include <std>
using namespace std;

int main() {
    console.log("FILES DEMO");
    console.log(join("count=", files.count()));
    console.log(join("exists=", files.exists("config.txt")));
    console.log(join("size=", files.size("config.txt")));
    console.log(join("line1=", files.read_line("config.txt", 1)));
    console.log(join("has vanilla=", files.contains("config.txt", "vanilla")));
    return files.read("config.txt");
}
