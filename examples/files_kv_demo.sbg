#include <std>
using namespace std;

int main() {
    string name = fileReadKeyValue("config.txt", "name");
    int level = fileReadKeyValueInt("config.txt", "level", 1);
    fileLoadKeyValuesToMap("config.txt");

    return join(name, join(" level=", join(level, join(" keys=", mapSize()))));
}
